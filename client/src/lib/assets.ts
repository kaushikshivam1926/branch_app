// Shared asset imports — Vite inlines these as Base64 data URIs at build time,
// ensuring the standalone index.html works without any external files.
import sbiLogoUrl from '../assets/sbi-logo.png';

export { sbiLogoUrl };
